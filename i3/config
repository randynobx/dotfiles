# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Workspace names:
set $ws0 0
set $ws1 1:ssh
set $ws2 2:www
set $ws3 3:mail
set $ws4 4
set $ws5 5
set $ws6 6
set $ws7 7
set $ws8 8
set $ws9 9
set $ws10 10:trans

# Monitor var
set $mon0 HDMI-0
set $mon1 VGA-0



###############################################
# Multi-Monitor setup
###############################################

#workspace $ws1 output $mon0
#workspace $ws2 output $mon1
#workspace $ws3 output $mon0
#workspace $ws4 output $mon0
#workspace $ws5 output $mon0

#workspace $ws6 output $mon1
#workspace $ws7 output $mon1
#workspace $ws8 output $mon1
#workspace $ws9 output $mon1
#workspace $ws10 output $mon1

###############################################

 set $mod Mod4

 font for window titles. ISO 10646 = Unicode
 #font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
 font -misc-dejavu sans mono-medium-r-normal--12-0-0-0-m-0-iso10646-1
 font -schumacher-clean-medium-r-normal--12-*-*-*-*-*-*-*

 #new_window border_style 1pixel

 for_window [class="Chromium"] border 1pixel
 for_window [class="Firefox"] border 1pixel
 for_window [class="Jumanji"] border 1pixel
 for_window [class="Keepassx"] border 1pixel
 for_window [class="MPlayer"] border 1pixel
 for_window [class="sxiv"] border 1pixel
 for_window [class="xournal"] border 1pixel
 for_window [class="Zathura"] border 1pixel
 for_window [class="mupdf"] border 1pixel
 for_window [title="mplayer2"] border 1pixel
 for_window [class="Roxterm"] border 1pixel
 for_window [class="Sakura"] border 1pixel
 for_window [title="Steam"] border 1pixel
 for_window [class="Thunderbird"] border 1pixel
 for_window [class="Virt-manager"] border 1pixel

 for_window [title="Calculator"] floating enable
 for_window [class="sxiv"] floating enable
 for_window [title="Mixer"] floating enable
 for_window [title="mplayer2"] floating enable
 for_window [title="Steam"] floating enable
 for_window [title="pdfedit"] floating enable

 for_window [window_role="pop-up"] floating enable

 # Colors {{{

 # class border bg fg
 client.focused #5b605e #5b605e #262626
 client.focused_inactive #3f3f3f #3f3f3f #5b605e
 client.unfocused #3f3f3f #3f3f3f #5b605e
 client.urgent #dca3a3 #3f3f3f #dca3a3

 client.background #000000

 # }}}

# assign programs to specific workspaces
assign [class="Firefox"] 2:www
assign [class="Transmission"] 10:trans

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# i3-input trigger
bindsym $mod+Shift+I exec /bin/i3-input

# scratchpad
bindsym $mod+Shift+O move scratchpad
bindsym $mod+o scratchpad show

# screenshot with PrintScrn
bindsym Print exec /usr/bin/scrot

# lock screen with i3lock [-d to disable screen on lock] [-i for custom bg image]
bindsym Ctrl+$mod+l exec i3lock -d #-i /home/randy/pictures/lockscreen.png

# start a terminal
#bindsym $mod+Return exec roxterm
bindsym $mod+Return exec sakura

# kill focused window
bindsym $mod+Shift+Q kill

bindsym $mod+F1 exec i3lock /home/randy/pictures/lockscreen.png
# increase volume in alsamixer
bindsym $mod+F3 exec amixer set Master 3+ unmute
# decrease volume in alsamixer
bindsym $mod+F2 exec amixer set Master 3- unmute
# pause ncmpcpp
bindsym $mod+F11 exec ncmpcpp toggle
# next song in ncmpcpp
bindsym $mod+F12 exec ncmpcpp next
# previous song in ncmpcpp
bindsym $mod+F10 exec ncmpcpp prev


# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# start quickswitch.py
bindsym $mod+a exec quickswitch.py
bindsym $mod+Shift+A exec quickswitch.py -s
bindsym $mod+Shift+D exec quickswitch.py -m

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
#bindsym $mod+h split h
bindsym $mod+u split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+m focus parent

# focus the child container
bindsym $mod+c focus child

# switch to workspace
bindsym $mod+grave workspace $ws0
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# misc workspace options
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev
workspace_auto_back_and_forth yes

# move focused container to workspace
bindsym $mod+Shift+grave move workspace $ws0
bindsym $mod+Shift+exclam move workspace $ws1
bindsym $mod+Shift+at move workspace $ws2
bindsym $mod+Shift+numbersign move workspace $ws3
bindsym $mod+Shift+dollar move workspace $ws4
bindsym $mod+Shift+percent move workspace $ws5
bindsym $mod+Shift+asciicircum move workspace $ws6
bindsym $mod+Shift+ampersand move workspace $ws7
bindsym $mod+Shift+asterisk move workspace $ws8
bindsym $mod+Shift+parenleft move workspace $ws9
bindsym $mod+Shift+parenright move workspace $ws10

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # They resize the border in the direction you pressed, e.g.
        # when pressing left, the window is resized so that it has
        # more space on its left

bindsym l resize shrink left 10 px or 10 ppt
bindsym h resize grow   left 10 px or 10 ppt

bindsym k resize shrink down 10 px or 10 ppt
bindsym j resize grow   down 10 px or 10 ppt

bindsym j resize shrink up 10 px or 10 ppt
bindsym k resize grow   up 10 px or 10 ppt

bindsym h resize shrink right 10 px or 10 ppt
bindsym l resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
bindsym Left resize shrink left 10 px or 10 ppt
bindsym Shift+Left resize grow   left 10 px or 10 ppt

bindsym Down resize shrink down 10 px or 10 ppt
bindsym Shift+Down resize grow   down 10 px or 10 ppt

bindsym Up resize shrink up 10 px or 10 ppt
bindsym Shift+Up resize grow   up 10 px or 10 ppt

bindsym Right resize shrink right 10 px or 10 ppt
bindsym Shift+Right resize grow   right 10 px or 10 ppt

        # back to normal: Enter or Escape
bindsym Return mode "default"
bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

#Special keys
bindsym XF86AudioRaiseVolume exec amixer set Master 1%+
bindsym XF86AudioLowerVolume exec amixer set Master 1%-
bindsym XF86AudioMute exec amixer set Master toggle
bindsym XF86AudioPlay exec mpc toggle
bindsym XF86AudioPrev exec mpc prev
bindsym XF86AudioNext exec mpc next
bindsym XF86Calculator exec galculator

#set dual monitors
#exec /home/randy/double_screen.sh
<<<<<<< HEAD
=======
exec xrandr --output DVI-0 --mode 1440x900 --primary
exec xrandr --output VGA-0 --mode 1440x900 --left-of DVI-0
>>>>>>> 14e5d421effed35594a3c4ea234fe4824c815dcb

# Set compose key map
exec xmodmap ~/compose_keymap

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
#    output          LVDS1
    status_command  i3status --config ~/.i3/i3status.conf
    position        bottom
    workspace_buttons   yes
    tray_output primary
}

exec i3status -c .i3/i3status.conf | i3bar -d
#exec conky -c /home/randy/.conkyrc | dzen2 -fn 'DejaVu Sans Mono-9' -ta r -dock -xs 1
#exec conky -c /home/randy/.conkyrc | i3-wsbar -c "dzen2 -x %x -dock"

# Set wallpapaer
exec feh --bg-scale ~/desktop_background.jpg
exec xcompmgr -o 80 & disown
